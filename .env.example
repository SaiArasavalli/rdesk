# Firebase Configuration
# Get these values from your Firebase project settings
# https://console.firebase.google.com/

# Toggle between Firebase Emulator and Real Firebase
# Set to 'true' to use Firestore Emulator (for local development)
# Set to 'false' to use real Firebase project (for production)
VITE_USE_FIREBASE_EMULATOR=false

# Emulator Configuration (only used when VITE_USE_FIREBASE_EMULATOR=true)
VITE_FIREBASE_EMULATOR_HOST=localhost
VITE_FIREBASE_EMULATOR_PORT=8080

# Real Firebase Configuration (only used when VITE_USE_FIREBASE_EMULATOR=false)
VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef

# Instructions:
# 1. Copy this file to .env.local
# 2. For local development with emulator:
#    - Set VITE_USE_FIREBASE_EMULATOR=true
#    - Run 'npm run emulator' in a separate terminal
#    - Run 'npm run dev' in another terminal
# 3. For production with real Firebase:
#    - Set VITE_USE_FIREBASE_EMULATOR=false
#    - Replace all placeholder values with your actual Firebase configuration
# 4. Never commit .env.local to version control
